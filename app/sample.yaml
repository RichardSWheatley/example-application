# This file provides example sample configurations for building the application.
# Multiple combinations can be specified so you can build different variants
# locally or in CI.
sample:
  description: Example application
  name: example-application 
common:
  build_only: true
  integration_platforms:
    - apollo510_mini
tests:
  app.default: {}
  app.debug:
    extra_overlay_confs:
      - debug.conf
  app.mcuboot:
    sysbuild: true
    tags: mcuboot
